# Node Config
MIDGARD_NODE_URL=http://localhost:3000
CARDANO_NETWORK=Preview
TX_INTERVAL_MS=5000

# Transaction Settings
ONE_TO_ONE_TXS=10    # Number of simple transactions per cycle
COMPLEX_TXS=10       # Number of complex transaction pairs per cycle (each creates 2 actual txs)

# Optional
OUTPUT_DIR=generated-transactions  # Output directory for storing transactions when node is offline 